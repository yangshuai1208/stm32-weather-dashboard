# Day7 系统主循环整理与裸机任务调度

## 1. 今日目标

在 Day1~Day6 已完成 OLED、DHT11、EC11、LED 等基础功能的基础上，整理 `main.c` 主循环结构。

Day7 不新增硬件，重点是把当前项目从“功能堆在 while(1) 里”优化成“结构清晰的裸机调度”。

## 2. 今日完成内容

- 整理 `main.c` 中的系统状态变量
- 将环境数据改为全局状态变量 `g_weather_data`
- 将当前页面改为全局状态变量 `g_current_page`
- 增加 OLED 刷新标志 `g_oled_need_refresh`
- 封装页面切换函数
- 封装 EC11 事件处理函数
- 封装 DHT11 周期采集函数
- 封装 OLED 刷新函数
- 保持 LED 舒适度指示功能正常
- 保持 EC11 页面控制功能正常
- 保持 DHT11 周期采集功能正常

## 3. 为什么要整理主循环

前几天为了快速验证功能，代码主要集中在 `while(1)` 中。

随着模块增多：

```text
OLED 显示
DHT11 采集
EC11 扫描
LED 状态指示
页面切换
舒适度计算
